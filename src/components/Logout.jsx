export const Logout = () => {
  return (
    <main>
      <h1>Logout</h1>
      <section>
        <span>Möchtest du dich ausloggen?</span>
        <div>
          <button>ausloggen</button>
        </div>
      </section>
    </main>
  );
};

export default Logout;
